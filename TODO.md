# TODO / 残タスク

## 未着手（2026-08-26 CEO 起票）

### 1. タイル間の「区切り線 + 付箋」でブロック分け
- 背景: CEO は 15〜24 列のタイルを並べ、似た仕事同士を隣接させてブロック化している（「返答待ち」「重いので後で」「今やってる」など）。今はブロックの境界が視覚的に分からない
- 要件:
  - タイルとタイルの間に 1px 程度の色付き縦線を挿入できる
  - 線に短いラベル（付箋）を付けられる。線の意味を書く場所
  - 線は移動できる（ドラッグ or 隣へ送る操作）
  - タイルの並び替え・追加・削除に追従して線の位置が保たれる
  - レイアウト同様 localStorage（`grid_v2`）に永続化
- 設計メモ: グリッド状態は `src/components/gridTabs.ts` の cell 配列がすべて。線は「cell の前に置く separator」として同じ配列に持たせるのが最小変更（`hole` と同じ発想で `parseGridState` に型を足す）。IA 上は「セル」ではなく「境界の注釈」なので、TerminalCell とは別コンポーネントにする

### 2. ペイン単位の「次に渡す指示」キュー — 実装済み（2026-08-26、plans/feat-next-instruction-queue.md、E2E 確認済み）
- 残: 送信は 1 行に潰される（terminalInput の sanitize）。複数行指示をそのまま送りたくなったら sender 側の方針から見直す
- 追加要件（CEO 2026-08-26）: 自動投入すると直前の完了報告が画面から消えるので、投入のたびに直前のやりとりを未読ログとして退避し、読めるまでバッジで示す
- 背景: 大きな作業をさせている最中に「あ、これも積んでおいて」と割り込むと、Claude Code がドリフトする・性能が落ちる。終わってから投げたいが、思いついた時に書き留めないと忘れる
- 要件:
  - タイルごとに指示を複数キューできる（順序付き、編集・削除可）
  - そのセッションのターンが終わった（入力待ちになった）タイミングで先頭の 1 件を自動投入する。投入は既存の draft-injection（貼り付け → Enter）を使う
  - 自動投入 ON/OFF を切り替えられる（手動で「今送る」も可）
  - キューの残数がタイルのヘッダに見える
- 設計メモ: サーバ側に `server/session/agent-prompt-queue.ts`（`POST /api/workspace/column` の初回プロンプト用、cwd キー・TTL 120s）が既にある。これを「session id キー・TTL なし・複数件」に拡張し、`/api/hook`（Stop イベント）で入力待ちを検知して次を流す形が既存機構に乗る。UI は TerminalCell のヘッダにキュー入力欄
- 関連: `POST /api/broadcast`（全セッション一斉送信）は「今すぐ送る」の実装参考
