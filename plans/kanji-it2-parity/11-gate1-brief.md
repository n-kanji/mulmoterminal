# 11 — 朝ブリーフ: 夜間実装完了 → Gate 1 ドッグフーディング開始可能

**生成モデル:** Claude Fable 5 (claude-fable-5) / 2026-08-01 早朝
**読む深さ:** 📙 要判断 — これだけ読めば Gate 1 を始められる

## 結果

計画した実装（Wave 0〜3、P0+P1 の全 11 Issue）は**全て完了し、本番サーバーに反映済み**。
http://localhost:34567 を**リロードするだけ**で新機能が使えます（サーバー再起動済み・既存 7 セッションは無傷で再アタッチ確認済み）。

- 品質ゲート: lint 0 エラー / typecheck 3 種 clean / **テスト 5,027 件 green**（開始時 4,678 → +349、ほぼ全新機能にスペック付き）
- 検収方式: 各ワーカーの自己申告を信用せず、マージごとに Layer 1 が全ゲート再実行 + diff 実読。実画面スクショと新 API の生存確認まで実施
- コミットは kanji ブランチにローカルのみ（**push は未実施** — 承認後に実行します）

## 入った機能（1 行ずつ）

1. **ワークスペース** — ページに名前（タブをダブルクリック）と固定（右クリック）。`?ws=名前` で別ウィンドウ別ワークスペース。**1 ページ 10 列**に拡大
2. **mission 層** — セルごとの大目的を常時表示（curl で書く。CLAUDE.md ゲートの差し替えは Gate 3 で）
3. **状態語 6 値** — 承認待ち / 質問 / 実行中 / 完了・未読 / 切断 / シェル（待機は「無表示」に）+ 放置時間で dot が黄→赤
4. **既定キーマップ** — Alt+J/L=列移動, Alt+U/H=ページ, Alt+A=要対応へ, Alt+Z=ズーム, Alt+N=新規, Alt+W=閉じる
5. **OS 通知** — タブが裏でも承認待ち/質問をポップアップ + favicon バッジ + 音の作り分け（画面を見ている時は鳴らさない）
6. **コピー** — 応答/プロンプトのワンクリックコピー（`…` 行）+ 選択即コピー（pbcopy 運用の廃止候補）
7. **Fork ボタン** — `…` 行から 1 クリックで会話を分岐した新カラム
8. **エージェント自走 API** — Claude 自身が新カラムを開く / 全セッションに一斉送信（CLAUDE.md 差し替え文面は plans/08 §5 にドラフト済み）
9. **スクロールバック 10,000 行 + 再折返し** — iTerm2 で原理的に無理だった「後から広げても読める」が有効
10. **積み残し一掃** — チップ削除 Undo / 打鍵中のソート保留 / ペインのカスタム名（ステータス行ダブルクリック）/ セル全面ドロップ + Cmd+V 画像貼付 / Shell セルの新ヘッダー
11. **事故防止** — 自動承認（tmux-monitor）が MulmoTerminal 側セッションも監視するよう本番修正済み

## ▶ Gate 1（半日ドッグフーディング）の手順

1. ブラウザをリロード → 通知許可を 1 回だけ求められたら許可
2. workspace-1 相当のプロジェクト群をプリセットから開いて通常業務
3. **iTerm2 を触った瞬間に理由をどこかにメモ**（それが次の Issue になる）
4. 最初の 5 分で見てほしい未検証項目: ①Alt+J/L が実キーボードで効くか ②OS 通知が裏タブで届くか ③Cmd+V 画像貼付 ④Fork ボタン ⑤`cct`（Agent Teams）をセル内で 1 回起動

## ⚠️ 既知の未検証・制限（正直な線引き）

- 実ブラウザでの E2E は smoke（描画・API 生存・スクショ）まで。**キーボード実機・通知の実表示・D&D の実挙動は jsdom では証明できない** → 上の 5 項目
- タブ行は 2 ページ以上ないと出ない（1 ページ目の命名は 2 ページできてから）
- Fork は「一度も発言していないセッション」では押下後にエラー表示（事前に無効化できない）
- 30 ペイン負荷実証（R9）は Gate 2 で実施

🔴 要判断（急ぎではない）: kanji ブランチを origin へ push してよいか。推奨は push（バックアップ・全 20 コミット）。理由: ローカルのみだとマシン障害で夜間分が全損するため。
