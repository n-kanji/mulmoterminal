# HANDOVER — ページを別ウィンドウに切り離す（2026-09-14）

## 何ができるようになったか

グリッドの**ページ（タブ）を独立したブラウザウィンドウに取り出して、1枚目と同時に見られる**ようになった。
不要になったら元のウィンドウに戻して結合できる。**セッション（claude / shell のプロセス）は
切り離し・再結合を通じて生きたまま**（PTY はサーバー側の tmux にあり、ソケットを繋ぎ替えるだけ）。

コミット `7d31b4fb`（park のアカウント落ち修正）/ `24c3d2cf`（本体）/ `b08cbfa4`（自己レビュー修正）、ブランチ `kanji`。
設計と判断の根拠は `plans/feat-detach-page-window.md` に全部書いてある。**続きをやる人はまずそれを読む。**

## 使い方

- **切り離す**: タブ行の `open_in_new` ボタン（見ているページを切り離す）。新ウィンドウが `?ws=<名前>` で開く
- **戻す（別窓から）**: 切り離した側のタブ行の「元に戻す」。書き置きを残して自分を閉じ、元の窓が取り込む
- **戻す（元の窓から）**: タブ行に出る**破線のゴーストタブ**をクリック。別窓は自分で畳む
- 元の窓が閉じている間に戻した場合は、次に開いたときに回収される

## 仕組み（要点だけ）

localStorage を使ったウィンドウ間の受け渡し。`storage` イベントは**書いた窓以外**に届くという仕様が、
そのまま「相手に知らせる」チャンネルになる（ポーリング不要・自分の書き込みとの競合なし）。

| キー | 持ち主 | 中身 |
|---|---|---|
| `grid_v2:<ws>` | 各ウィンドウ | 切り離されたページのグリッド。`origin` に元の窓のキーを持つ |
| `<元の窓のキー>::detached` | 元の窓 | 貸し出し中ページの台帳（ゴーストタブの正体） |
| `<元の窓のキー>::home:<ws>` | 別窓が書く → 元の窓が読んで消す | 帰宅便 |

判断は全部 `src/components/gridDetach.ts` の純粋関数（テスト 38 件）。
localStorage と `window.open` の副作用だけ `src/composables/useDetachedPages.ts`（ビュー越しのテスト 14 件）。

## 触る前に知っておくべきこと

1. **サーバーは1セッション1ソケット**。後から繋いだ窓が勝ち、前のソケットは `superseded` で切られる。
   だから切り離しは「元のグリッドから取り除く」が必須。逆に言うと順序は気にしなくてよい（常に新しい窓が勝つ）
2. **`conn.terminate` を絶対に呼ばない**。PTY を殺す。切り離し後の後始末は `conn.release`（ソケットを閉じるだけ）で、
   しかも**新しい窓が引き継ぐ猶予として 5 秒待ってから**。ソケットを閉じるとサーバーの 30 秒回収タイマーが走り出す
3. **自動整列（auto sort）中は切り離せない**。auto は待機中のペインをページを跨いで並べ替えるので、
   「画面の2枚目」と `pageSlice(state.cells, 1)` が一致しない。理由はツールチップに出る
   （このフォークは IT2_MODE で並び替えトグル自体が非表示なので実害はない）
4. **ページの meta は index で cells と対応している**。ページを抜くときは `pages[page]` を**splice で抜く**こと。
   空にするだけだと、後ろから詰めてきたページが抜いたページの名前とアカウントを着てしまう
5. **付随物を運び忘れない**。ページのアカウント・ラベル、セルの name / width / account / agent / launcher。
   落とすと別窓のペインが既定アカウントで起動する（同じクラスのバグが `parkCell` にもあった → `7d31b4fb` で修正）

## 実機検証（2026-09-14 済み・使い捨て `?ws=detachcheck` で実施）

シェルを2ページに1つずつ起動 → 2枚目を切り離し → 新窓で同じ tmux ペインにコマンドが届くことを確認
（`DETACH-MARKER-1`〜`4` が1つのペインに時系列で並ぶ）→ 「元に戻す」で回収 → もう一度切り離して
ゴーストタブから呼び戻し（別窓は自分で閉じた）→ 戻ったペインに再びコマンドが届く。
台帳・子のキー・帰宅便はいずれも後始末済み。検証で作った tmux セッションは kill 済み。

## 残タスク

- [ ] リリース時: `docs/ChangeLog.md` + `docs/guide/{en,ja}/v<version>.md`（リポジトリ規約。`/publish` の手順）。
      切り離し・戻し方・別窓を閉じても消えないこと、を書く
- [ ] `origin`（spacengine/mulmoterminal）への push は未実施（`company` remote）
